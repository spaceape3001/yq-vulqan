add_subdirectory(bigempty)
add_subdirectory(hello)
add_subdirectory(imdemo)
# add_subdirectory(camera) # Disabled for tagging
add_subdirectory(textking)
# add_subdirectory(asteroids)
if(EXISTS composite)
    #add_subdirectory(composite)
endif()

